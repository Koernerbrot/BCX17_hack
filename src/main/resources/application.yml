app:
  version: @project.version@

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vibreserver
    username: root
    password:
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update